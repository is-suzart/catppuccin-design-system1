{
  "name": "catppuccin-design-system-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/showcase",
    "build": "npm run build --workspaces --if-present"
  }
}
