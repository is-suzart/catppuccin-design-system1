{
  "name": "catppuccin-design-system-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun --filter catppuccin-showcase dev",
    "build": "bun --filter \"*\" build"
  }
}
