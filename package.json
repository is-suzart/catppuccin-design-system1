{
  "name": "catppuccin-design-system-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "yarn workspace catppuccin-showcase dev",
    "build": "yarn workspaces foreach -A run build"
  },
  "packageManager": "yarn@4.17.1"
}
